# @package _global_
defaults:
  - override /agents: minimal
  - override /docker: cpu_only

# Quick test configuration
model: gpt-4o-mini
max_iterations: 3
dry_run: false

# Override Hydra output directory for tests
hydra:
  run:
    dir: ./outputs/tests/${now:%Y-%m-%d}/${now:%H-%M-%S} 